# Memória — 2026-04-19

---

## Integração Google OAuth2 Direta (sem Composio)

### O que mudou
Composio foi removido definitivamente. Substituído por OAuth2 direto via script Python próprio.

### Arquivos
- `/root/.openclaw/credentials/google_oauth.json` — client secret OAuth (type: installed app)
- `/root/.openclaw/credentials/google_tokens.json` — access + refresh token (token_expiry: timestamp em ms)
- `/root/.openclaw/credentials/gog.py` — script Python com get_access_token(), gscQuery(), refresh automático

### GSC OAuth scopes
```
https://www.googleapis.com/auth/webmasters.readonly
```

### Fluxo de autorização
1. Client secret → `google_oauth.json`
2. Code da URL → trocado por tokens → `google_tokens.json`
3. Access token expirado → refresh automático via `gog.py`

### GSC sites acessíveis (24 sites)
Todos os sites Santtas + nichos.

---

## GSC Live Reports — IMPLEMENTADO

### Spec
`/root/.openclaw/workspace/docs/superpowers/specs/2026-04-19-gsc-live-reports-design.md`

### API Route
`GET /api/clients/[id]/gsc?type=monthly|queries|pages&from=YYYY-MM-DD&to=YYYY-MM-DD&limit=N`

**Resposta:**
```json
{
  "rows": [...],
  "totals": { "clicks": N, "impressions": N },
  "lastUpdated": "ISO",
  "requestsToday": N,
  "gscPropertyUrl": "sc-domain:..."
}
```

### Arquivos novos
- `mission-control/src/app/api/clients/[id]/gsc/route.ts` — API route
- `mission-control/src/app/api/debug/route.ts` — debug tokenexpiry

### Arquivos alterados
- `mission-control/src/app/(dashboard)/clients/[id]/reports/page.tsx` — 4 seções GSC live

### Tracking de uso
`/root/.openclaw/credentials/gsc_usage.json` — contador de requisições por dia (data, requests, lastReset)

### 4 Seções do Report
1. **Tráfego Mensal** — monthly GSC, KPIs + tabela com vs mês anterior
2. **Páginas Principais** — top 20 pages por cliques
3. **Consultas Principais** — top 20 queries por cliques
4. **Keywords** — mantido do DB (não muda)

### Cada bloco tem
- Badge "🔵 Google Search Console"
- Badge de uso: "⏱ Atualizado há X min • N requisições hoje"
- Filtro de período (date inputs "De" / "Até")
- Botão refresh manual
- Estados: loading / sem dados / erro / não configurado

### GscUsageBadge
- Normal: texto muted
- `requestsToday > 8000`: amarelo + ⚠️

---

## Campo gsc_property_url no DB

**Tabela:** `clients`
**Tipo:** `TEXT` (nullable)

Cliente Santtas (id=1): `sc-domain:br.santtas.com`

---

## Pendências

- [ ] Adicionar `gsc_property_url` na tabela `clients` (mudar de TEXT se já existir)
- [ ] Testar GSC live reports no Mission Control (Santtas)
- [ ] Revogar token Composio antigo: `ck_Ttjh1cjSB6j58vZ0TRwN` (console.cloud.google.com)

---

## Lições

- `rowLimit: 1` na API GSC retornava só 1 linha — resultado 25.478 vs real 1.867.119
- OAuth "installed app" não passa pelo consent screen se não adicionar test users no Cloud Console
- GSC Search Analytics API não expõe headers de rate limit — tracking feito localmente com JSON

---

## Skill multi-search-engine-2 instalada

- Busca em 17 search engines sem API keys
- Usar quando Osmar pedir "pesquisa de keywords" ou "pesquisa de concorrentes"
- Path: `/root/.openclaw/workspace/skills/multi-search-engine-2/SKILL.md`
